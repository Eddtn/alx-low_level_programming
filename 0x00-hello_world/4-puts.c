#include<stdio.h>
/**
 * main- Serves as the entry point for this program
 *
 * RETURN: By default returns zero if no erros otherwise returns non-zero value.*/

int main(void)
{
	puts("\"programming is like building a multilingual puzzle"):
		return(0);
}
